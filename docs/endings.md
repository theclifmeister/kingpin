# The endings, the exit plans and the run summary

Each ending well is also a plan the player can see and pin before it opens (#347, `docs/ambitions.md`): the bar reads full exactly when the condition below holds.

**A run ends nine ways** (#49, §7; `game/exit.go`, `content/endings.toml`, `ui/summary.go`, `ui/exit.go`; the three the run had, #195's `retired`, and five this issue added).
`Ending.Cause` is the kind, one of `content.Causes` (`CauseIndicted` ..
`CauseVanished`; no second `Kind` field, the coordinator's ruling), `Ending.Who` the name in it where there is one, and **every writer goes through `World.End(cause, day, who)`**, which stamps `Stats.Score` as the world stands and returns the `Ending` the owner assigns to `w.Over`.
Each ending is **detected by the sim that owns its condition, in its step, as a threshold on state it already reads, with no dice**, written as the heat sim writes the indictment; `TestSimsWriteOnlyTheirOwnState` lists `Over` for the four owners (heat, crew, rivals, laundering) instead of sharing it, and a zero in the owner's file boxes the detector (`harness.NoEndings` boxes all four), so a run on the file before the endings is the run it was.

| Cause | Owner | Detected | Tuning |
|---|---|---|---|
| `indicted` | heat | the DA's file at `evidence_arrest` after the fall guys, as before | `heat.toml` |
| `arrested` | heat | the arrest rung after the fall guys: since #475 a warrant signed the night the line is met and served the next on a sale or the heat held (`docs/law.md`) | `heat.toml [[response]] arrest` |
| `broke` | crew | no stock anywhere and less cash, dirty and clean together (#395: clean is drawn back at a fee, `World.CashOut`), than the cheapest unit; the offshore account is not in it | `laundering.toml [cashout] fee` |
| `retired` | the player: `World.Retire` (#195) | `Offshore >= retire_cash` and `QuietDays >= retire_days` | `laundering.toml [offshore]` |
| `businessman` | laundering | `LegitIncome(w)` (the fronts' own income net of upkeep, #192) over zero and over the street (the more of the night's street revenue, the tick's `PlayerSold`, and its average night over the run, `World.StreetAverage`, #493) with home's goodwill over its pressure, `legit_days` days running (`World.LegitDays`, this sim's; a day that fails zeroes it) opens going straight (#398), taken from the walk-away dialog while it holds (`World.GoStraight`) | `laundering.toml [businessman] legit_days` = 30 |
| `kingpin` | rivals, then the player: `World.Crown` (#227) | `Dominant()` (#43) held `dominant_days` days (the day it began is the latest of the table's own stamps, `World.DominantSince` since #347, which `Sim.DominantSince` delegates to: `Absorbed`, `Fragmented`, the homage deal's `Since`, so no counter) while you hold more than `kingpin_share` of home's corners (`Sim.HoldsTheCity`) stamps the reign (`World.Reign`); the crown is taken from the walk-away dialog while it holds | `rivals.toml [endings]` `dominant_days` = 14, `kingpin_share` = 0.5 |
| `taken_out` | rivals | a faction's push took the last corner you held anywhere tonight (`LastFlip == t.Day`, `Held() == 0`), its war with you is open (`War >= war_threshold`, or since #478 it is the war you declared on it, `World.War` as the night began), and fewer enforcers are on the payroll than `taken_out_muscle` | `rivals.toml [endings] taken_out_muscle` = 2 |
| `betrayed` | crew | a lieutenant running a city turns informant (the flip, #31: loyalty under `flip`, no dice) while that city holds `betray_share` of your corners and `betray_corners` at least | `crew.toml [lieutenant]` `betray_share` = 0.5, `betray_corners` = 3 |
| `betrayed` | rivals | a faction breaks a deal with you of its own accord (`whim`, a chaotic one's `betrayal`) while another faction's war with you is over `betray_war` | `rivals.toml [endings] betray_war` = 40 |
| `vanished` | heat, or the player: `World.Vanish` | the indictment or the arrest the fall guys do not take, with a new identity from the tree (`fx.Identities`, `Sim.exit`); or `Vanish(fx)` on any morning with one | `upgrades.toml identity` |

**The exit plans.**
The offshore account (#195) is the first: what is in `World.Offshore` survives every ending, and only that (no `offshore` node, no `offshore_share`: #118 took the id and #195 made the account a number the player sets).
The new identity is the second: `identity`, a Legal-branch node beside `retainer` and `fallguy` (`upgrades.toml`: $4M clean, requires `retainer`, `identities = 1`, `Effects.Identities` summed, `TestFoldEffects`), enables `World.Vanish(fx)` (`ErrNoIdentity` without one; `CanVanish`) and turns the end into an exit: the heat sim runs the plans **in order where the indictment is written** (`heat.Sim.exit`: the fall guy takes the first, once each, wiping the file, dropping every city's heat to `heat.toml` `fall_heat` (50) and costing `fall_cash` (half) of the dirty cash and of the clean, `heat.Sim.takeFall`; then the identity makes the cause `vanished`; then the end), and since the run is over either way the identity is spent once.
`TestExitPlansRunInOrder`: on one seed and tree the aggressive trader is arrested on day 18; with the identity it vanishes on day 18; with a fall guy it is arrested on day 25 after the fall; with both it vanishes on day 25 (17 and 23 before #475: each arrest now waits a night behind its warrant, the aggressive trader selling through it); the account reads the same through all four.
`harness.BuyUpgrades` never buys the identity (an exit is a decision a policy makes, not the cheapest node left in Legal), so the boss's and the distributor's runs are the runs they were; a test that wants it owns it (`Own`).

**The reign** (#227): Kingpin is a crown you take, not a trapdoor that closes the run.
The rivals sim's detector stamps `World.Reign` (the day the reign began, sticky; 0 while none, the run before) the morning its condition first holds and emits one `events.ReignBegan{City, Crews, Homage}` (a `rivals` headline, `ReignBegan` in `headlines.toml`; the report's TIER section opens with `The city is yours: every crew in Eastside is gone or paying. Take the crown when you are ready (walk away on the dashboard), or reign.`; the dashboard's ALERTS carry `The city is yours: day 31 of the reign, 4 crews paying $41,200 a night. Take the crown or play on.`, keyed once, so a fast-forward stops the morning it begins and not again; `stopEvent` names `ReignBegan` too).
While it holds and you play on, every morning's TIER opens with `REIGN: day 31 of the reign · 4 crews paying homage · $41,200 a night` (`news.reignLine`: the crews are the homage deals live, `World.HomageDeals`, the money what the tick's `TributePaid{ToYou}` paid tonight; `every crew gone` with none paying) and the STREET facts' corners line carries `· reign d31` (`World.ReignDay`, the first morning day 1; on the corners' fact because the tier's is the first the panel drops for room).
The morning the condition stops holding (a homage ends and the faction sets up again, or the share falls) the stamp zeroes, `events.ReignBroken{Why}` (report-only: `The reign is over for now: a crew set up again.` / `the city slipped under the share`) opens the TIER section, the fast-forward stops, and it can begin again `dominant_days` on.

**The grace and the churn** (#399): a holding that hovers on the share broke the reign and began it again every few days, each time a stop and a report (the playtest on `d2ec6d2`). Since #399 a reign whose share slips rides it `rivals.toml [endings] reign_grace` (3) mornings before it breaks (`World.ReignSlip`, the rivals sim's, counting them; the city held again zeroes it with no event), and a crew setting up again still breaks it at once. While it is slipping the crown waits (`World.CanCrown` false, `World.Crown` refused with `ErrReignSlipping`), the REIGN line ends `· slipping under the share` and the dashboard's reign alert turns to `The reign is slipping under the share of the corners: it breaks in 2 mornings unless you take corners back.` `ReignBegan` carries `Again` (`World.Reigns` counts the reigns the run has begun), so the fast-forward stops on the run's first reign only and the report says `The city is yours again: every crew in Eastside is gone or paying, and the crown is open.` for the rest. The walk-away dialog's closed crown row says what is short in the kingpin plan's own steps (`3 of 6 corners held, 1 crew still standing`, or `day 9 of 14` once both hold, `ui.crownShort` over `Session.Ambitions`) where it said `the city is not yours`. Guards: `TestReignBreaks` (the grace ridden, the crown refused while slipping, the city held again, the break past the grace, `Again` on a second reign), `TestReignStopsOnce` (`engine`), `TestFastForwardStopsOnTheReign` (the closed row's words). `TestSeedDigest` moved on day 1 by shape alone (the two fields); nothing reigns in a harness run but the kingpin scenario, which crowns the morning the reign begins, so no band or frequency moved.
No day cap, nothing ends a run for playing on (#27).
**The walk-away's first page** (#463) is a table of the ways out, `open` or `not yet`, each with its `terms`, while closed what is `short`, and since #478 what it `scores` (`the account: $420,000`, or `the account over 1 + 1 body: $210,000`: the same on every row, since every ending scores the account) on rows of their own under it (`viewExit`, `TestEveryWayOutSaysItsScore`), the cursor's way out followed into view: a column of terms beside a column of shortfalls was cut to `$750,000 s…` and `3 of 6 cor…` at 80 columns (`TestModalsNeverCutProse`).
**The crown** is the walk-away dialog's third row, `Take the crown` (`ui/exit.go`, `World.CanCrown`: `day 31 of the reign` while it holds, `the city is not yours` greyed out and refused otherwise, the digit `3` selects it); its confirmation (`TAKE THE CROWN?`, `y crown`) ends the run `kingpin` through `World.Crown` and `End` exactly as `Retire` and `Vanish` do, on the day it is taken.

**Going straight** (#398) is the businessman ending as a claim, the way #227 made Kingpin one: the laundering sim no longer ends the run the night the streak reaches `legit_days` (a playtest's Kingpin campaign ended a businessman on day 285 without being asked). It reports the night the ending opens (`StraightOpened`) and the night it lapses (`StraightLapsed`), both report-only and both fast-forward stops (`engine.StopsOn`, cue `run` with phase `straight` or `lapsed`); while it holds the dashboard's ALERTS carry `The fronts earn $1.2M a day, more than the street: go straight (walk away) or play on.` (`engine.AlertStraight`, keyed once), and the walk-away dialog's fourth row, `Go straight` (the digit `4`), reads `the fronts at $1.2M a day` when open and `the fronts out-earn the street 30 nights` with `3 of 30 nights so far` in the refusal when not; `y` on its confirmation ends the run through `World.GoStraight` and `End`. The harness claims it the night it opens in its own loop (`harness.run`, after the night's `EndDay`), which is where the sim ended the run before, so every seed-pinned number, band and ending frequency is the one before (`TestEveryEndingIsReachable`'s businessman row still ends on day 30); `harness.Straight(cfg, p)` claims it the morning after for a loop of its own (`TestAmbitionProgressAgreesWithTheEnding`). Guards: `TestGoingStraightIsClaimed` (`sim/laundering`: the streak never ends the run, opens once, lapses once, the claim refused closed and taken open), `TestGoStraightFromTheDialog` (`ui`), `TestModalsFit` (`walk away: straight?`).
The summary's `reached` row then reads `Kingpin on day N` (the reign's first morning, `reachedLine`) and the epilogue names the reign lived (`.Reign`, `31 days`, `content.Epilogue`: `The city was yours 31 days before you took the crown, and the crown lasted 4 years.`, the years the function of heat and pressure they were).
The score is unchanged, `Offshore / (1 + Bodies)`, so playing the reign on is allowed and never rewarded (`TestCrownIsTheScoreAsItStands`: the kingpin scenario crowned 0, 10 and 30 days into the reign scores the same and the summary's day is the crown's).
Guards: `TestNoEndingIsTheOldRun` holds (the boss never reaches `Dominant()` by day 200), `TestEveryEndingIsReachable`'s kingpin row is the reign beginning on day 15 and the crown taken the same morning (`harness.Crowned(Idle)`), `TestReignBreaks` (`sim/rivals`: the stamp `dominant_days` on, the crown open, a homage ending zeroes it and closes the crown, a new deal begins it again, the share falling breaks it, the sim never writes `Over`), `TestFastForwardStopsOnTheReign` (`ui`: the stop, the report, the dashboard, the second fast-forward running past it, the crown from the dialog, the summary; the row refused with no reign), `TestModalsFit` (`walk away: crown?`).
`TestSeedDigest` moved on day 1 by shape alone (`World.Reign` in the walk; with the field skipped the digest is the list before it on all sixty days); `TestSimsWriteOnlyTheirOwnState` lists `Reign` under the rivals sim; no band or seed-pinned number moved.

**The score** (#195's ruling): `Stats.Score = Offshore / (1 + Stats.Bodies)`, `World.Score()`, stamped by `End` the morning the run ends.
The pile left behind is printed, never scored; days are shown, never scored; so lying low k more days before an exit can never raise it (`TestRetireeRetires`, k = 0/10/30/60: 765,314 on seed 1 at every k) and the bodies divide it (`TestScoreIsTheAccount`).
`cmd/balance` prints `endings: map[cause: runs]; N of M ended, scoring $X (median)` (the median over the runs that ended; a run still going on the horizon has no score), documented in `docs/harness.md`.

**The summary** (`ui/summary.go`, `viewOver`) replaces the game-over table: the one modal titled `<TITLE> · DAY N`, the ending's title in `theme.Good` for a run won (`endings.toml won`) or `theme.Bad` for one lost with the day and the tier reached, the **epilogue** (a `text/template` from `endings.toml` over `content.Epilogue`: `.Days`, `.City`, `.Here`, `.Offshore`, `.Left`, `.Bodies`, `.Fronts`, `.Corners`, `.Name`, `.Leader`, `.DA`, `.Chief`, `.Pages`, `.Years`, `.Hot`; the kingpin's is a function of heat and pressure at home the morning it ends and nothing else: `[kingpin] reign_years` × (1 − (heat + pressure) / 200), rounded, a year at least, `KingpinConfig.Reign`, and `.Hot` picks who finished it), **THE STORY** (`[summary] lines` headlines of the run by the `[summary.weight]` of their source, the paper's notoriety in the reputation sim's sense, shown in the order they happened as `day N  text`; since #465 a text is told once, at its latest (five identical patrol lines were a story), and the run is cut into as many equal spans of days as there are lines, each giving its heaviest headline, the latest among equals, a span with nothing to tell leaving its line to the heaviest of the rest: before, the latest first among equals told a 563-day run from days 524 to 551 (`TestStoryTellsTheWholeRunOnce`); the headline templates `[summary] quiet` names are never in it, `LaidLow`'s "Nothing to report from Eastside corners" among them, a heat line that counts toward notoriety and pressure as one, since #424: a run that lay low to its end told its story in five quiet nights, `Model.quietLines`, `SummaryConfig.validateQuiet`, `TestStoryIsNotTheQuietNights`), **THE MONEY** (the account marked `· the score`, the pile left behind in dirty, clean and units, peak wealth and revenue, the wash and the seizures and the losses, the levels), **THE PEOPLE** (the bodies and #46's fallen spelled `Name (role · day N)`, the betrayals: informants, defectors, members a faction poached (`Stats.CrewPoached`, #465: `betrayals none` after a runner took a faction's offer), lieutenants who walked, deals broken by them and by you; the best of the crew: the first of the fallen, else the member whose tenure × loyalty is highest), **THE CITY** (the three reputation bars on one row, the ground and the busts (`N won · N lost`: won is rival corners your enforcers took, lost every corner that left you against your will, `Stats.CornersLost`, since #465 counted where it happens: a faction's push or price war, the crackdown clearing yours, a corner nobody worked going back to the street, a lieutenant walking with one to nobody; before, only a faction's take counted, and a run that lost six read `1 lost`; a corner you give up yourself never counts; `TestIdleCornersDrift`, `TestTipsAndCrackdown`, `TestSummaryCountsTheBetrayalsAndTheGround`), the law with the chief's temper as you know it, #45) and the **SCORE** line with the account, the bodies, the days and, since #50, where the run ranks among your own (`· 3rd of 12 runs`, `tied 1st of 2 runs` since #498 where another run has the same score, `rankLine`, `docs/profile.md`; the first line carries the daily's date and what the run unlocked in gold, `unlockedLine`); `Model.finish` is every path to `modeOver` and files the run in the profile once.
It scrolls at 80x24 (`↓ more`) and stands whole at 120x40 for every cause on a run with the fallen, the levels and the betrayals to print (`TestSummaryReadsTheRun`); `TestRendersAtCommonSizes` and `TestNoKeyHintsOutsideTheLegend` walk `rich game over <cause>` for every cause; `TestEndedSaveReloadsToTheSummary` saves a run ended by each cause and continues it straight to the summary with the epilogue rendered; `TestEveryEndingHasAnEpilogue` (`content`) walks the causes against the file, which `validate` checks at load (one row a cause, a title, an epilogue that parses, a scene line, `[summary] lines` and `[kingpin] reign_years` at least one).
`Ending`, `score` and `walk away` are help WORDS, and since #472 so is `quiet day`: `all heat under 40; no strike, push, bust or buyer's order`, the number read off `laundering.toml [offshore] retire_heat` (the laundering sim's `quiet`: every city under it, no `Enforcement`, `CornerStruck`, `RivalPushed` or `WarEscalated` that night, no live buyer contract). It describes; nothing on the screens pushes toward retiring (#413).

**A decision per ending** (#478, the playtest's question whether each exit is a real choice; ruled on the issue, respecting #413: nothing pushes toward one ending).
- **The score is unchanged.** Every ending scores the account (#195), so no ending is worth more than another: the ending decides the story and the unlock. The tester's $48K vanish against a $755K retirement was the size of the account, not a vanish penalty. Each walk-away row now says what it scores.
- **Vanish is confirmed** as the exit that needs no terms, priced in the upgrade tree. Its plan gains the lawyer on call it was missing (`docs/ambitions.md`). Carrying the clean cash into the score was not taken: it would push players toward one ending.
- **Taken out is kept**, as losing a war you could not hold, and the war condition stays: dropping it would make taken out a trapdoor. A war you declared now counts as open, and the declaration and the rivals pane warn while you have fewer than `taken_out_muscle` enforcers (`docs/rival.md`; `TestADeclaredWarIsOpen`, `TestWarWarnsShortOfMuscle`). Measured with `cmd/balance`, 20 runs x 300 days, before and after: `war`, `warlord`, `saboteur`, `reckless`, `tipster`, `crewed`, `managed` and `quiet` end exactly as before (only `warlord` and `reckless` declare a war, and neither lost its last corner to that faction short of muscle), so no frequency or pin moved.
- **Kingpin: the share stays.** The playtest's hidden clock was factions yet to arrive, which block `Dominant()`; the closed crown row now counts them apart (`2 crews yet to arrive`), beside #472's per-faction lines in the plan and its corners held against the share.
- **Retire: an opt-in sweep** removes the manual transfer chore (`docs/laundering.md`): off by default, no balance change.
- **Businessman is confirmed** (#398).

**Going straight takes a real business** (#493, ruled on the issue, respecting #413: the rule describes the ending, nothing pushes toward it). A playtest went straight on day 77 on one Laundromat level (+$420 a day), one goodwill purchase and thirty nights laid low: a night laid low sells ~$0, and any front income out-earned it, so "the fronts out-earn the street" read true on $39K of front earnings against $1.1M of street. Of the issue's three options the smallest sound one is to measure the street, not last night: a night now counts only while the fronts out-earn **the more of the night's street and its average night over the run** (`World.StreetAverage(day)`: `Stats.TotalRevenue`, the street, the buyers and the exports, over the nights played; the laundering sim's `legit` reads it at the tick's day, `Session.AmbitionTerms` at the morning's, so the plan's `income` step and the count read one number). Not counting a lie-low night alone was not taken: a night that simply sells nothing is the same $0, and the ending would still open on any income. A run's peak night was not taken either: one lucky night would close it for good. The average falls as the nights pass without dealing, so a front business the size of the street you ran opens it, and one Laundromat level after $1.1M of street would need ~2,600 nights. A run that never sold reads a street of $0, as before (the harness's businessman scenario and `TestGoingStraightIsClaimed` are unchanged). The plan's step reads `$420/day against the street's $14,286 a night` (`stepWords`; it said `against $0 last night`). Guards: `TestLyingLowIsNotGoingStraight` (`sim/laundering`: the playtest's thirty nights after $1.1M of street count none; the same fronts in a run that never sold open it), `TestLieLowStreetIsZero` (`ui`). Measured with `cmd/balance`, 20 runs x 400 days: `boss` ends as before, a businessman on 4 of 20 (its levels out-earn its average night by then; the output is identical line for line); `cartel` a businessman on 8 of 20 where it was 13 (the runs whose streak ran against a slow night now wait for the fronts to beat the average night; the median score of the runs ended $11.4M to $13.0M), and nothing else ends differently. The engine's policies reach it on the business, as the ending means.

**The crown waits on you, not on clocks** (#495, ruled on the issue, respecting #413). A playtest held all ten corners at home by day 52 and waited: a run-out faction's `gone in Nd` restarted every time it claimed a corner (`gone in 59d` again), and a sixth and a seventh faction, drawn to Bayport by the take, pushed the reign's start from day 263 to past 391. Three rulings, each a run that never meets its state being the run before:
- **A claim it does not keep is not somewhere to stand.** A run-out faction that claims a corner and loses it again inside `rivals.toml [factions] settle_days` (30) picks its landless clock up from the first rout (`RivalState.Spell`, `RunOut`); one that keeps a corner `settle_days` has settled and its next rout starts a new clock (`docs/rival.md`). 0 is the rule before, every claim restarting it.
- **No new faction is drawn while the crown waits only on clocks** (`rivals.Sim.CrownClock`): while you hold more than `kingpin_share` of home's corners and no faction still standing holds a corner (bar a run-out one on a claim it has not kept), the money draws no scouts. A faction already on its way still comes and a seat in the wings still moves in on its day; each is already on the rivals screen with its day.
- **The rivals screen lists each blocker with its clock**, as it has since #472 (`for the crown: ...` under each faction that does not count, and the crown's `crews down` step a line each); a faction on a claim it has not kept reads `holds 1 corner; run out 40d ago, the clock runs on unless it holds one 28d more`, and a run-out one `gone in 27d unless it keeps a claim 30d`.
- **One reign day on every screen.** The kingpin plan's streak read `days it holds: 48 of 14` (days since the table fell, capped at nothing) while the crown's row read `day 36 of the reign`: once the reign has begun the step is `game.UnitReign` and reads `World.ReignDay`, `days it holds: day 36 of the reign`, the number the walk-away row, the REIGN line and the dashboard give (`TestTheReignReadsOneDay`).
Guards: `TestAClaimNotKeptKeepsTheClock` (`sim/rivals`: a claim lost after 5 nights scatters the faction `absorb_days` after its first rout, as `Down` says the morning it loses it; one kept `settle_days` starts a new clock), `TestNoNewFactionWhileTheCrownWaits` (the scouts not sent while it waits, sent once the faction has settled), `TestCrownSaysWhatKeepsACrew` (`ui`: the words). `TestSeedDigest` moved on day 1 by the shape of `RivalState.Spell` alone (with it left out of the walk all sixty days are main's); `TestEveryEndingIsReachable`, `TestExitPlansRunInOrder`, `TestEndingFrequencies`, `TestMoneyCurve` and `TestOneFactionIsTheOldRun` hold unchanged. Measured: `-policy warlord`, 20 runs x 400 days, 77 absorbed to 86, `dominant on` 2,537 days to 3,838, day-300 net worth $1.01M to $1.34M; `-policy war` 74 absorbed to 78, `dominant on` 445 days to 1,142; neither ends differently or reaches the crown (neither holds the share long: the kingpin tester's ten of ten is not a scripted policy).

**The scene** (#156, `docs/animation.md`): `causeScene` plays the three of #156 for their causes and **`anim.Exit(title, line, accent, rng)`**, the one new shape, for the six after them: the cause's title from `endings.toml` decrypts in the accent (`theme.Money` won, `theme.Heat` lost: `overAccent`, the player's accent too) over 0.7 s, then its `scene` line prints under it over 0.8 s, `OverLength` in all; registered once as `over:exit` over the retiree's text (`cmd/anim -scene over:exit`; `DemoScene("over:<cause>")` plays any cause in the file).
An unknown cause still plays the arrested scene (`TestEndingSceneByCause` walks `content.Causes` and one nobody has).

**Walking away** (`ui/exit.go`, `w` on the dashboard since #245, `modeExit`): one modal with two pages that asks twice.
The first lists the three ways out with their terms and whether each is open (`Retire`: `retire_cash` offshore and `retire_days` quiet, with what is short; `Vanish`: a new identity; `Take the crown`: the reign, #227 above), the cursor on the first open one; `enter` on an open one turns to the confirmation (a closed one is refused with what is short on the dialog's own error line, `Retire is not open: $750,000 short, 10 more quiet days.` in `Bad` under the refused row (#494; under the table before), `exitDialog.err`, cleared by the next key: #468, the status bar under the modal made `enter` read as doing nothing), `shift+tab` goes back, `y` ends the run there and then (any other key declines, as every confirmation does, #241) through `laundering.Sim.Retire` or `World.Vanish`, saves, and opens `modeOver` on the ending's scene as a morning would (`confirmExit`); `esc` closes.
`TestWalkAwayAsksTwice`, `TestModalsFit` (`walk away`, `walk away: retire?`, `walk away: vanish?`, `walk away: straight?`, `walk away: pending`); a refusal sits under the row it refuses, in view with it (#494: the longer terms pushed a line at the page's foot out of view at 100x30); the day does not step (`TestWalkAwayAsksTwice`).
Its first page's `a ambitions` opens the ambitions panel (#347, `docs/ambitions.md`).

**Settled before it is scored** (#494, a playtest: $19.7M reserved offshore in one night, `394 pages due tomorrow`, and the crown taken the next morning before the pages were read, Kingpin at $9.5M; the same worked for retiring). The heat sim reads a move over the lot on the tick after the laundering sim records it (`Laundering.Structured`, stamped with the tick's day, which is the next morning's), so the morning after a lump its pages are still due. `World.PagesDue(perLot)` is that count (the lots over the line times `heat.toml structure_evidence`, 0 when the last move was not last night or fit the lot; `Session.PagesDue` at the file's rate) and **every walk away waits on it**: `Session.Retire`, `Vanish`, `Crown` and `GoStraight` refuse with `game.ErrPagesDue` (`the DA reads last night's transfer offshore tonight: 19 pages go in the DA's file first, so end the day`, `Session.settled`), and the dialog's open rows close with the pages as what is short, its first page leading with `Last night's transfer offshore puts 19 pages in the DA's file tonight: end the day first.` in red (`pendingLines`). The day ended, the pages are filed (a file past `evidence_arrest` ends the run indicted, the fall guy and the identity taking it as they would any indictment), and the walk away opens the morning after. The score itself is unchanged by design (every ending scores the account, #478): what the pages decide is the ending. Refusing was the smallest of the issue's two options: reading the pages as filed at the walk away would run the heat sim's exit plans outside its step. The session is the check, not `World`, because the rate is the heat sim's tuning; `harness` policies move a lot a day at most (`ReserveLot`), so no policy meets the refusal and no number moved. `PagesDue` is not on the wire (`protocol.unserved`): the refusal names the pages, and the report warned of them the morning before.
**The reverse trap**: a reserve made this morning is out of the pile at once and lands in the account only tonight, so a walk away now neither scores it nor leaves it behind (the go-legitimate tester lost $3,780). The dialog's first page and the confirmation list it in amber, `$3,780 lands offshore tonight: end the day first, or it is neither scored nor left behind.`, and let the walk away go ahead: it is money the player chose to move. Guards: `TestALumpIsReadBeforeTheWalkAway` (`engine`: three lots refuse all four exits the morning after, the night files the pages and retiring then takes; twenty lots end the run indicted the night the pages are read), `TestWalkAwaySaysWhatIsPending` (`ui`).

**Each ending in its own words** (#498). Going straight confirmed with the vanish's copy (`Vanish on the new identity with $22,395 offshore`); it has its own now, `Go straight on 3 fronts: the fronts at $1.2M a day, the street given up, $22,395 offshore. The DA's file goes to the archive; the run ends now, on day 77.`, and its footer lists `y go straight` beside `y retire`, `y vanish` and `y crown` (it listed no `y` at all). The terms on the first page are the plans' (`docs/ambitions.md`): `Go straight` reads `the fronts out-earn the street and goodwill tops pressure at home, 30 nights running` (the goodwill was left out) and says which fails this morning under `short` (`0 of 30 nights so far, the fronts under the street, goodwill under pressure`, `straightShort`); `Take the crown` reads `more than 50% of home's corners and every crew gone or paying, 14 days running` (`kingpin_share`, `dominant_days`); `Vanish` names the lawyer before the papers. A ready ending raises its alert once, keyed so a fast-forward stops once: retiring's alert, keyed `retirement` short and ready alike and so never a stop when it opened, is keyed `retirement open` once ready; vanishing gains `engine.AlertVanish` (`vanishing`, the dashboard: `The papers are good: vanish on the new identity with $4.2M offshore (walk away) or play on.`); the reign and going straight had theirs. Guards: `TestEachEndingConfirmsInItsOwnWords`, `TestWalkAwayTermsAreThePlans`, `TestAReadyEndingAlertsOnce` (`engine`).
**The ending takes a deliberate key.** A tester mashing `esc`, `↓` and `enter` through the morning's reports went past the ending unread and quit from the slot menu. The morning a run ends (`Model.finish(true)`, `Model.overFresh`) the summary ignores `esc` and lists `M menu`, which leaves for the start menu; a summary seen before (an ended save continued) closes on `esc` as it did. `TestTheEndingTakesADeliberateKey`.
**The score spelled out.** The SCORE line reads `SCORE  $756,000  the offshore account $756,000 ÷ (1 + 0 bodies) · 77 days · 1st of 2 runs` (`scoreWords`; `$0 over 1 + 0 bodies` was opaque), adding `: nothing was moved offshore, and only the account scores` with an empty account; the slot menu shows an ended run's score (`docs/saves.md`); the businessman's epilogue says the fronts were `out-earning the street by the end` (`earning more than the street ever did` was false for a run whose fronts earned $39K against $1.1M of street). `TestScoreLineSpellsItOut`.

**The harness** (`harness/endings_test.go`): `TestEveryEndingIsReachable` is the table of nine scripted scenarios, one a cause, each reaching exactly that cause on seed 1 and the same day the second time (the aggressive trader indicted on day 17; every city's heat pinned at 100 with decay 0 arrested on day 2, the warrant signed on day 1 (#475); no cash and no stock broke on day 1; the account and the quiet days in hand retired; three fronts at the top of their ladders, the city on side and nothing sold a businessman on day 30; every faction fallen and six of home's ten corners held the reign on day 15 and the crown the same morning (#227); a lieutenant on four corners at loyalty `flip` − 1 betrayed on day 1; you alone on one corner against an expansionist with twelve heads and a war at 60 taken out on day 14; the identity owned vanished on day 23, a day later behind the warrant); `TestExitPlansRunInOrder` above; `TestEndingFrequencies` over fifty seeds (`aggressive` indicted on 50, `retiree` retired on 34, `quiet`, `managed` and `crewed` never kingpin or a businessman: `TestNobodyIsDominantByAccident` pins `Dominant()` false for sixteen policies and `TestNoEndingIsTheOldRun` logs the boss's legit days peaking at 2 of 30 on seed 1, 3 over fifty seeds: its levels earn ~$0.3M a day against ~$0.9M of street and it runs the city at pressure 100); `TestNoEndingIsTheOldRun` hashes the boss, the laundered and the distributor players daily to day 200 on the file and on the file with `NoEndings`, the count of legit days set aside as #195's quiet days are, and pins them identical, nothing ending; `TestScoreIsTheAccount`.
`TestSeedDigest` moved on day 1 by shape alone (`World.LegitDays`, `Ending.Who`, `Stats.Score`): with the three skipped the digest is 2be03a7's on all sixty days, and `cmd/balance -incidents off -trace` prints main's boss, laundered and distributor traces to the dollar to day 200.
No band moved: t1–t4 $84,930 / $544,231 / $16,399,563 / $90,250,452.

**Rulings and why.**
`kingpin_share` is not in the issue: with the incident table on (the tool's default), #44's `rival_leader_killed` emptied a three-faction table over an idle player in about a year (until #389, when the incident began handing the faction to a successor instead of fragmenting it), and a quiet trader standing on one corner read kingpin on 8 of 50 seeds by day 300 (the harness's `TestEveryIncidentFires` ran the idle player into it on eight seeds); a city nobody holds has no kingpin, so the reign needs more than half of home's corners, which the tipster never holds (its seed 7 read kingpin at 5 of 10 before the share) and the boss holds on one seed in fifty by day 211 with the weather on, the mechanic and not an accident.
`betray_corners` is not in the issue either: the boss's seed 6 collapsed to one corner with an unpaid lieutenant on it, and a flip over one corner is a leak, not a betrayal.
`taken_out` counts the enforcers on the payroll, laid up or not (muscle you have), because the UI's rich fixture, a hand-built war with one head of muscle, was taken out on a seed in twenty.
The kingpin's epilogue is deterministic in the heat and the pressure (`Reign`), never the dice.
`GameOver` is emitted by every owner and the clock breaks the order on it, so the news sim never sees it: report-only by construction.
